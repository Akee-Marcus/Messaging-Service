services:
  app:
    image: app:latest
    build:
      context: .
    ports:
      - "${APP_PORT:-80}:80"
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
